# Visualizer For Scraped Data
A dynamic visualizer for webscrapped data.

## Requirements
```
$ pip install plotly==4.2.1
$ pip install pandas
```
Depending on the installation use pip3 or your python-3 compatible pip service.
